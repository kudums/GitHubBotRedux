// /*const userEnteredData;
// const responseFromRecastAPI;
// const reponseFromGitHub;*/

// export const createRepo = (state = { widgetLst: [] }, action) => {
//   if (action.type === 'CREATE_REPO') {
//     state = { widgetLst: [...state.widgetLst, action.payload] };
//   }
//   return state;
// };

// /*export const createIssue = (state = [], action) => {
//   if (action.type === 'CREATE_ISSUE') {
//     state = {widgetLst: [...state.widgetLst, action.payload] };
//   }
//   return state;
// };

// export const editIssue = (state = [], action) => {
//   if (action.type === 'EDIT_ISSUE') {
//     state = {widgetLst: [...state.widgetLst, action.payload] };
//   }
//   return state;
// };

// export const getIssue = (state = [], action) => {
//   if (action.type === 'GET_ISSUE') {
//     state = {widgetLst: [...state.widgetLst, action.payload] };
//   }
//   return state;
// };

// export const addUser = (state = [], action) => {
//   if (action.type === 'ADD_USER') {
//     state = {widgetLst: [...state.widgetLst, action.payload] };
//   }
//   return state;
// };

// export default { createRepo, createIssue, editIssue, getIssue, addUser };*/
// export default { createRepo };
